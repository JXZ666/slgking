# 更新日志

## v0.21.1（2026-09-22）

紧急修复：VPN/代理环境下目录同步失败、封面下载卡死。

- **同步直连服务器**：目录/数据库/封面下载改走无代理直连（`ProxyHandler({})`），绕过用户 VPN/代理对裸 IP `43.130.240.89:8080` 的吞请求（502/超时），解决「同步失败服务器不可用」与封面逐个卡 30s 的假死。

## v0.21.0（2026-09-22）

目录同步从「客户端抓站」改为「服务器下发」，界面与稳定性收尾。

- **服务器目录同步**：客户端改从作者服务器拉取预构建目录（`43.130.240.89:8080`），不再直连 dikgames，避免用户 IP 因抓取被封。合并按 slug 对齐，只写站点目录，用户评分/备注/收藏/译文一概不碰。
- **我添加的游戏**：可手动添加站上没有的游戏（`origin='user'`），只出现在「我添加的游戏」视图，填齐后可勾选加入主列表。
- **本地目录匹配**：扫描时支持中文标题匹配（CJK 感知的版本号/噪音剥离 + 中文/英文双索引 + 别名）。
- **一键部署**：`deploy_server.py` 把爬虫部署到腾讯云并装好每日增量同步 cron；`serve_catalog.py` 提供快照发布 + HTTP 目录服务。
- **标签多列 + 默认中文**：添加/编辑游戏的标签复选框从单列改为流式多列，文字走中文译名（无译名才回退英文），按中文名排序。
- **交流群移位**：右上角工具栏的交流群小卡移除，群号统一放在游戏详情区**下方**、与底部翻页栏齐平的一条里，为将来右上角登录等 UI 腾出空间。
- **启动动画重做**：加载卡片改成 Canvas 逐帧绘制 —— 扫描线扫过、logo 外圈进度环、分段式进度格、标题字符跳变、闪烁光标。
- **同步超时加固**：目录清单拉取超时 15s→30s，加 3 次重试退避，弱网下不再误报「服务器不可用」。

## v0.20.5（2026-09-21）

更多工具弹窗可滚动，新增头像、更新推送与本地评论。

## v0.20.0（2026-09-20）

收藏夹改造与删除，全屏详情按钮撑满，反馈群号 1124074040。

## v0.19.0（2026-09-19）

预置游戏数据，汉化工具改版，界面修复。

## v0.18.0（2026-09-18）

列表改每页 7 张分页，修热度计数正则并加回填，右上角重排。

## v0.17.5（2026-09-18）

补齐热度维护项，README 加 B 站宣传视频。
