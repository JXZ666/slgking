# 更新日志

## v0.22.8（2026-09-24）
- **评论接口隐私与防滥用**：管理员密钥与设备身份改用请求头传输，不再放进 URL；补充发评频率限制、重复举报拦截，并在客户端明确显示删除/举报结果。启动时不再查询评论身份；公开评论会先校验服务端的字数与请求体限制。
- **头衔展示优化**：重做头衔徽章质感与稀有度动效，减少持续闪烁；个人头衔页和商城支持直接预览。
- **想玩清单更新提醒**：为尚未安装的想玩游戏记录本地版本基线；官网版本更新后显示新旧版本，可逐条或全部标记已读。
- **隐私说明校正**：README 明确区分本地资料、匿名使用统计和用户主动公开的评论。

## v0.22.7（2026-09-24）
- **商城展示加宽**：打开积分商城时临时扩大商品区域，让常用窗口下能同时看到多张商品卡；切回游戏或个人中心时恢复原布局。
- **暗色主题调整**：以深梅紫、玫粉霓虹、酒红和香槟金营造成熟的赛博朋克氛围，保持文字与按钮清晰可读；浅色主题和详情页布局不变。

## v0.22.6（2026-09-24）

- **云端评论失败可见**：加载失败会提示并可重试；公开评论上传失败会保留在本机、标记待公开并可重试。
- **管理员统计补充空间状态**：查看评论与匿名上报日志大小、更新时间、服务器磁盘余量及最近可识别的备份文件。
- **磁盘状态提示修正**：服务器未部署新版接口时会明确提示，不再误报为磁盘读取失败。
- **版本补偿修正**：从较新的测试版切回旧版本时不重复发放更新补偿积分。

## v0.22.4（2026-09-23）

- **云端评论上线**：评论区新增「公开发布」开关，公开的评论会同步到服务器、所有人都能看；别人的公开评论可举报，自己发过的公开评论可删除。默认仍是「仅自己可见」。
- **成就头衔**：新增「收藏家」（收藏夹攒满 20 款游戏自动解锁）与「鉴赏家」（公开发表满 10 条评论自动解锁），达成时顶部提示。
- **代码瘦身**：清理一批死代码与冗余逻辑，删除仓库里的临时日志文件。

## v0.22.3（2026-09-23）

- **评价与评论合并**：详情页不再有「评价」和「评论」两块，合并成一个评论区——输入框就写在评论区上方，写完点「发布」，原「写评论」弹窗取消。原有的评价内容会自动迁进评论区，标记为「仅自己可见」，不会再出现在别处。
- **个人页操作按钮被裁（修复）**：小窗口下「修改昵称 / 查看头衔 / 兑换码」第三个按钮会被挤出面板，要拉宽窗口或全屏才看得见。改为按面板宽度自适应，同时收紧个人页纵向间距，最小窗口下三个按钮也都在首屏里完整可见。
- **热度行评论数改名**：改成「官网评论 x」，标明那是抓官网的数据，不再和本机评论区同名。帮助文档与备份说明里原先写的「备注」也一并改称「评论」，并在「卡片与详情」补了一段说明评论区怎么用。
- **评论可见性字段**：评论新增「仅自己可见 / 公开」标记，默认仅自己可见，为云端评论做准备。

## v0.22.2（2026-09-23）

- **本地游戏可二次编辑**：自添加的游戏不再写死，详情面板新增「编辑此游戏」，可改标题/开发商/引擎/版本/简介/标签/本地目录/是否加入主列表，写错无需删除重加。
- **软件内一键更新**：发现新版本后弹窗可直接「立即更新」，自动下载新 exe、退出后覆盖重启；正式版 `slgking.exe` 走 GitHub 资产自动更新，测试版无对应资产时回退到浏览器下载页。
- **兑换码入口上移**：个人页「操作」段（修改昵称 / 查看头衔 / 兑换码）从面板底部移到「我的数据」正下方，小窗首屏即可见，不再需要滚到底。
- **移除分享截图**：删除游戏详情页的「分享截图」按钮与截图保存逻辑。
- **接入域名**：客户端服务器地址切到 `https://slg-king.com`（原 IP 直连改为 Cloudflare 代理的正式域名），封面缓存省流由 Cloudflare 缓存规则承接。

## v0.22.1（2026-09-23）

- **快捷启动**：本地已下载的游戏与自添加的游戏，可选定启动 exe，在软件内一键启动（详情面板「启动游戏」，右键可重新选）。
- **信息栏精简**：左上角不再显示「简介缺 N 款（同步补齐）」——站点本就没有简介的条目，再同步也不会补齐，不再用这行误导用户。

## v0.22.0（2026-09-22）

本地归属感批次：头衔、积分、签到，外加同步修复与界面微调。

- **头衔系统**：普通/稀有/史诗/传说/至臻五档稀有度（规则不对外公开，只通过颜色与特效体现）。默认「普通用户」，另有「资深用户」「首发用户」「群友」「幸运星」，以及只靠抽奖 0.01% 概率出的传说头衔「幸运之王」。
- **积分签到**：每日签到 +10 分，签到记录与积分账本本地持久化。
- **积分商城**：用积分兑换头衔（资深用户 300 分、首发用户 30 分限时至 2026-10-30）；改名卡留待云端版开放（昵称仍免费改）。
- **每日群友兑换码**：「群友」头衔改为每日轮换兑换码，作者每日在交流群分发当天码（前一天码宽限到次日）。
- **个人中心**：工具栏「个人」入口不再弹窗，改为与「游戏详情」共用右侧面板（有游戏显示时暂时覆盖，点游戏卡片即切回）；面板内的修改昵称/查看头衔/兑换码仍走弹窗。面板排版收紧：收藏/评分/本地游戏/抽奖/积分收支合成一块两行数据卡，「我的记录」小节并入其中，三个操作入口从通栏三条改为横排一行，少滚一屏。
- **每日签到 · 积分商城独立入口**：右上角新增「每日签到」「积分商城」按钮，与「个人」并列，填满该行右侧。
- **同步绕过系统代理（修复）**：v0.21 目录改服务器下发后，开着梯子的用户请求被代理拦截，报「服务器不可用」、封面下载卡住。本次给作者服务器自身的请求绕过系统代理直连，并校验下发目录的 sha256。
- **个人按钮移位**：工具栏「个人」从右上角移到「排序」正下方预留空行，为将来右上角登录等 UI 腾位。
- **商城改版**：从「分组标题 + 每件商品一条」改成「左侧货架栏 + 自动换行的网格卡片」。货架栏上下分组（分类 / 细分），当前项用高亮竖条标出；商品卡固定尺寸，窗口越宽一行排得越多（最小窗口一行两张，最大化四到五张），商品再多也只是多几行。
- **商品卡细化**：卡面按稀有度描边并染底色，传说档带流光扫过、至臻档铭牌循环变色；已拥有的卡名字转暗、右上角挂「已拥有 / 使用中」状态角标；积分不够时价格下方直接写「还差 N 分」并配一条进度条，一眼看出距离。
- **兑换反馈**：购买或使用不再是弹一个确认框，而是当前卡片闪一下白，然后从顶部飞入一条结果提示条自动消失，操作留在原地不打断浏览。
- **抽奖横幅塌陷（修复）**：商城顶部抽奖横幅左边那条装饰竖条此前没有固定高度，把整条横幅撑到 200px，货架被顶到屏幕外。
- **抽奖动画重做**：三列符号卷轴连续滚动、缓动减速到停，停下时轻微回弹并震一下窗口，随后闪白与彩带爆发；抽中头衔时三列统一成该档的符号（幸运星三列全 7、幸运之王三列全皇冠）+ 金色全屏爆发 + 徽章飞入，抽中皇冠另有一段六音上扬的号角。转盘声与中奖音效默认开启，可在「设置」里关掉。
- **帮助文档重排**：从一条长滚动改成「左栏目录 + 右栏内容」，补齐收藏夹、个人中心/签到/积分/头衔、商城与抽奖、添加我的游戏、扫描本地目录、备份与恢复、更新推送等章节，并订正了「更多工具…」与「更多…」的位置描述。
- **封面 404 修复（服务器侧）**：服务器此前只有数据库、没有封面文件，新用户同步后封面全是灰方块，每次同步还会静默重试两千多个 404 请求。本次把封面补齐到服务器，并在部署脚本里加了「没有封面就补上」的一步，避免重装又回到 0 文件。
- **启动动画居中（修复）**：加载卡片改用与显示缩放同一套单位计算位置，不再偏在屏幕左上；卡片上的文字也随缩放一起放大，高分屏下不再偏小。
- **抽奖空框（修复）**：滚轮偶尔停成三个空框——卷带只铺满一屏，减速停在临界相位时整条带子被顶出画布。卷带改为首尾相接的周期带并加铺几格，任何相位都盖得住可视区；滚轮同时加长，起转补了加速段，停下时判定线闪一下。
- **奖项公开**：抽奖弹窗新增「抽奖概率」，逐档列出中奖组合、奖品与概率（两位小数，0.01% 才显示得出来），说明里画的图案就是滚轮上转的那批图标；777 为史诗头衔「幸运星」，三个皇冠为传说头衔「幸运之王」。原先与对照表对不上的一档运气擦边已取消，没中就是没中。
- **免责声明**：抽奖弹窗常驻一行说明——奖品仅为软件内积分与虚拟头衔，无货币价值、不可兑换现金或实物，与真实赌博无关。弹窗高度改为按内容算、按钮与这一行固定在底部，以前窗口写死 600 高、这行正好落在窗口之外只看得见半行。
- **统计口径**：数据统计里的「活跃设备」订正为「累计设备（去重）」，并新增近 14 天的每日事件/设备视图；面板内可直接复制当天的群友兑换码。
- **商城商品显示不全（修复）**：默认窗口下部分商品被挤到面板可视区之外，看起来像商品不见了。商品卡宽度改为随面板宽度自适应，默认窗口一行两张，拉宽窗口自动加列。
- **商城稀有度补齐**：分类「头衔类」与「全部」的细分栏列出全部五档稀有度，当前没有商品的档次进去如实显示「暂无商品」；已拥有的商品排到后面。
- **头衔简介与获得方式**：每枚头衔补一句简介，点开可见完整简介与获取路径；弹窗改为「固定标题 + 可滚动正文 + 固定按钮」，文案再长（幸运之王那句最长）也不会把按钮顶出可视区或被右边缘切字。
- **查看头衔按稀有度分组**：普通/稀有/史诗/传说/至臻各一节，节标题带该档「已拥有 / 总数」，不必再从头翻到尾。
- **崩溃记录**：程序里任何回调抛异常都会往数据目录的 `crash.log` 追加一段带时间的完整堆栈，并只弹一次提示告诉用户日志在哪。窗口版没有控制台，以前这类异常连堆栈都留不下来。
- **开发者头像**：解锁开发者特权后，点个人中心的头像即可挑一张图作为自己的头像（圆形裁切，存在数据目录、不进仓库）；普通用户暂时没有这个入口，但设置头像的接口已经留好。
- **改档检测**：`points_log` 与 `owned_titles` 每行加一枚链式 HMAC 封印，密钥存在库外的 `integrity.key`。启动时校验，发现程序之外写入或改动的记录就把它排除出余额与头衔，并在个人中心显示「存档被外部修改」及被排掉的部分。这是门槛不是保险箱——防的是随手拿数据库工具改一个数，密钥在本机，抄走它重算整条链就能造出一份「干净」的存档。
- **签到日历可点名补签**：漏签日期以红色标出，点它即可用 5 积分补上那一天，不必再盲补最近一天。
- **抽奖保底与记录**：连续 100 抽未出头衔则第 100 抽必出「幸运星」；已拥有大奖头衔再抽到改为折算积分（幸运星 100，幸运之王 2000）；抽奖弹窗显示保底进度，个人中心新增「最近抽奖」记录。
- **至臻头衔特效重做**：从离散变色改为连续流光，个人徽章与商城卡面同步。
- **管理工具快捷方式**：快捷方式脚本现在同时创建主程序与管理工具两个桌面快捷方式。
- **抽奖每日上限提高**：每日抽奖从 1 次改为 3 次，商城与帮助文案同步更新。
- **版本更新补偿**：每次版本更新自动发一笔维护补偿积分，大版本 30 分、小版本 10 分。
- **弹窗层级修复**：概率弹窗、头衔弹窗改为贴在调用它们的窗口之上，不再偶发跑到主窗口或抽奖弹窗后面。
- **头衔弹窗稳定性**：头衔「获得方式」弹窗偶发崩溃的入口做了兜底。
- **收藏与个人中心互通**：个人中心「收藏」统计卡可点击直达收藏夹；游戏详情面板新增「返回个人」入口。
- **帮助文档补充**：说明签到、积分、头衔、抽奖这套社交属性是为后续云端评论版本铺路，不必急着攒。

## v0.21.0（2026-09-22）

目录同步从「客户端抓站」改为「服务器下发」，界面与稳定性收尾。

- **服务器目录同步**：客户端改从作者服务器拉取预构建目录（`43.130.240.89:8080`），不再直连 dikgames，避免用户 IP 因抓取被封。合并按 slug 对齐，只写站点目录，用户评分/备注/收藏/译文一概不碰。
- **我添加的游戏**：可手动添加站上没有的游戏（`origin='user'`），只出现在「我添加的游戏」视图，填齐后可勾选加入主列表。
- **本地目录匹配**：扫描时支持中文标题匹配（CJK 感知的版本号/噪音剥离 + 中文/英文双索引 + 别名）。
- **一键部署**：`deploy_server.py` 把爬虫部署到腾讯云并装好每日增量同步 cron；`serve_catalog.py` 提供快照发布 + HTTP 目录服务。
- **标签多列 + 默认中文**：添加/编辑游戏的标签复选框从单列改为流式多列，文字走中文译名（无译名才回退英文），按中文名排序。
- **交流群移位**：右上角工具栏的交流群小卡移除，群号统一放在游戏详情区**下方**、与底部翻页栏齐平的一条里，为将来右上角登录等 UI 腾出空间。
- **启动动画重做**：加载卡片改成 Canvas 逐帧绘制 —— 扫描线扫过、logo 外圈进度环、分段式进度格、标题字符跳变、闪烁光标。
- **同步超时加固**：目录清单拉取超时 15s→30s，加 3 次重试退避，弱网下不再误报「服务器不可用」。

## v0.20.5（2026-09-21）

更多工具弹窗可滚动，新增头像、更新推送与本地评论。

## v0.20.0（2026-09-20）

收藏夹改造与删除，全屏详情按钮撑满，反馈群号 1124074040。

## v0.19.0（2026-09-19）

预置游戏数据，汉化工具改版，界面修复。

## v0.18.0（2026-09-18）

列表改每页 7 张分页，修热度计数正则并加回填，右上角重排。

## v0.17.5（2026-09-18）

补齐热度维护项，README 加 B 站宣传视频。
